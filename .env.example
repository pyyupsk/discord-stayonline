# Discord user token (REQUIRED)
# Obtain from browser dev tools: Open Discord in browser → F12 → Network tab → Find request → Copy "authorization" header value
# WARNING: Using a user token may violate Discord TOS. Use at your own risk.
DISCORD_TOKEN=your_token_here

# API key for web UI authentication (optional)
# If set, users must enter this key to access the dashboard
# Generate a secure random key, e.g.: openssl rand -hex 32
API_KEY=

# HTTP server port (optional, default: 8080)
PORT=8080

# Allowed WebSocket origins (optional, default: localhost)
# Comma-separated list of allowed origins for WebSocket connections
# Example: http://localhost:8080,http://127.0.0.1:8080
ALLOWED_ORIGINS=
