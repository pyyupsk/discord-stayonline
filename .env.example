# Discord user token (REQUIRED)
# Obtain from browser dev tools: Open Discord in browser → F12 → Network tab → Find request → Copy "authorization" header value
# WARNING: Using a user token may violate Discord TOS. Use at your own risk.
DISCORD_TOKEN=your_token_here

# PostgreSQL connection URL (optional, recommended for cloud deployments)
# If set, config is stored in PostgreSQL instead of a file
# Example: postgres://user:password@host:5432/dbname
DATABASE_URL=

# API key for web UI authentication (optional)
# If set, users must enter this key to access the dashboard
# Generate a secure random key, e.g.: openssl rand -hex 32
API_KEY=

# HTTP server port (optional, default: 8080)
PORT=8080

# Path to config file (optional, only used if DATABASE_URL is not set)
CONFIG_PATH=config.json

# Allowed WebSocket origins (optional, default: localhost + same-origin)
# Comma-separated list of allowed origins for WebSocket connections
# Example: http://localhost:8080,http://127.0.0.1:8080
ALLOWED_ORIGINS=

# Discord webhook URL for status notifications (optional)
# Sends notifications when connections go down or recover
# Get from: Discord Server Settings → Integrations → Webhooks → New Webhook
DISCORD_WEBHOOK_URL=
